{
  "scripts": {
    "compile": "npx tailwindcss --config ./tailwind/tailwind.config.js -i ./tailwind/admin2.css -o ./static/admin2/css/admin2.css",
    "dev": "npm run compile -- --watch",
    "build": "npm run compile -- --minify"
  },
  "devDependencies": {
    "daisyui": "^2.51.6",
    "postcss-import": "^15.1.0",
    "postcss-nesting": "^11.3.0",
    "tailwindcss": "^3.3.2"
  }
}
